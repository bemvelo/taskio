# Taskio
> Get it done,simply and on track.

A full-stack To-Do application built with FastAPI (Python) and React (TypeScript).

## Setup

### Backend
cd backend
pip install -r requirements.txt
uvicorn main:app --reload --port 8000

### Frontend
cd frontend
npm install
npm run dev